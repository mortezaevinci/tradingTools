# documentation

| File | What it is |
|---|---|
| `ibtrader template setup.docx` | Setup for the IB trader template — the configuration behind [`../csharp/IB2`](../csharp/IB2) and its `IbTrader` project. |

Everything else is documented where it lives: each top-level folder has its own
`README.md`, and the root [`README.md`](../README.md) ties them together.

The reasoning behind the tools — strategies, observations, vendor research — is
not in this repository. It is in `C:\temp\_results\tradingtools\base ideas`,
along with the market data and order exports.
