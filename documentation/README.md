# documentation

| File | What it is |
|---|---|
| `ibtrader template setup.docx` | Setup for the IB trader template — the configuration behind `..\csharp\IB2\IbTrader`. |

The rest of the documentation is not here: each area carries its own `README.md`,
and the reasoning behind the tools is in `..\base ideas\`.
